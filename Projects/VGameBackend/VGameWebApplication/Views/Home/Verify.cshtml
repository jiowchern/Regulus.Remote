@{
    ViewBag.Title = "Verify";
}
<h4>登入</h4>
<div >

    @{
        using (Html.BeginForm())
        {
            _Page_Views_Home_Verify_cshtml.<div class="form-group">
                @Html.Label("帳號")
                @Html.TextBox("user")
            </div>

            _Page_Views_Home_Verify_cshtml.<div class="form-group">
                @Html.Label("密碼")
                @Html.TextBox("password")
            </div>

            _Page_Views_Home_Verify_cshtml.<input type="submit" value="確認" class="btn btn-default"/>
        }

    }
    @Html.ActionLink("Android : 魚機APK", "Download");

</div>